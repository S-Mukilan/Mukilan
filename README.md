# Appilo Landing

Project restructured into separated HTML/CSS/JS folders.

## Entry Point
- `index.html`

## Notes
- Vendor CSS/JS local files currently load CDN equivalents via `@import` / script injection wrappers.
- Replace placeholder images in `img/` with real assets as needed.
